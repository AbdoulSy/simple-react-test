describe("Given <Modal /> and `_children`", () => {
    describe("When rendered by a User", () => {
        it.todo("should show a modal on top of the current content with `_children` content inside")
    })
})