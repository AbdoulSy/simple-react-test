describe("Given <Button />", () => {
    describe("And with `label` and `onClick`", () => {
        describe("When rendered by a User", () => {
            it.todo("should show the expected `label`")
            describe("And a user clicks on <Button />", () => {
                it.todo("should call the `onClick` function")
            })
        })
    })
})